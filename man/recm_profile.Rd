% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{recm_profile}
\alias{recm_profile}
\alias{print.recm_profile}
\alias{plot.recm_profile}
\title{Profile the criterion in one parameter}
\usage{
recm_profile(object, which = 1, span = 3, ngrid = 25, level = 0.95)

\method{print}{recm_profile}(x, ...)

\method{plot}{recm_profile}(x, ...)
}
\arguments{
\item{object}{an object of class \code{recmfit}.}

\item{which}{integer, index of the parameter to profile.}

\item{span}{numeric, half-width of the grid in estimated standard errors.}

\item{ngrid}{integer, number of grid points.}

\item{level}{numeric(1) confidence level for the profile interval.}

\item{x}{an object of class \code{recm_profile}.}

\item{...}{further arguments passed to or from other methods.}
}
\value{
An object of class \code{recm_profile}, a list with \code{grid}, \code{value},
\code{a0}, \code{cutoff}, \code{estimate}, \code{level}, \code{name}, \code{interval} and
\code{rel_range}.
}
\description{
Re-optimises the remaining parameters at each point of a grid in one
parameter, using the \code{objective} closure stored on the fit. No
re-specification of the model is required.
}
\details{
\code{rel_range} is the range of the criterion over the grid, relative to its
minimum. Below 0.02 the parameter is effectively unidentified and the
print method says so. Expect it: at \code{m = 2} a fourfold change in
\code{k_1/k_0} cost 2.2\% of SSR, and the true parameters sat 1.7\% above the
optimum. This is inverse optimal control -- many cost configurations
rationalise nearly identical observed behaviour.
}
\examples{
set.seed(1)
n <- 160
dys <- as.numeric(stats::filter(rnorm(n, 0.0104, 0.004), 0.5,
                                method = "recursive"))
dat <- data.frame(ystar = cumsum(dys))
dat$y <- dat$ystar - 0.05 + cumsum(rnorm(n, 0, 0.002))
fit <- recm_estimate("y", "ystar", beta = 0.995, data = dat, m = 2,
                     var_lags = 2, restarts = 1, quiet = TRUE)
recm_profile(fit, which = 1, ngrid = 7)

}
\seealso{
\code{\link[=recm_boot]{recm_boot()}}, \code{\link[=recm_estimate]{recm_estimate()}}; \code{docs/02-math-spec.md}
section 10.
}
