% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{recm_estimate}
\alias{recm_estimate}
\title{Estimate a rational error correction model with polynomial adjustment
costs}
\usage{
recm_estimate(
  y,
  ystar,
  vars = NULL,
  beta = 1,
  data,
  m = 3,
  cost = c("geometric", "free"),
  expectations = NULL,
  var_lags = 4,
  diff_exp = TRUE,
  method = c("nls", "gmm"),
  instruments = NULL,
  iv_lag = NULL,
  free_forward = FALSE,
  growth = NULL,
  hac_lags = NULL,
  start = NULL,
  subset = NULL,
  maxit = 4000,
  restarts = 3,
  quiet = FALSE
)
}
\arguments{
\item{y}{character(1), column name of the decision variable, \strong{in levels}
(usually logs). The equation is estimated in first differences.}

\item{ystar}{character(1), column name of the frictionless target, in the
same units as \code{y}. Supplied by the user, typically from a cointegrating
regression. \strong{Its estimation error is not propagated} -- see
\code{\link[=recm_boot]{recm_boot()}} and roadmap item R-1.}

\item{vars}{formula or character vector of extra regressors, entering
linearly with free coefficients. Concentrated out of the optimiser.}

\item{beta}{numeric(1) discount factor. \strong{Calibrate this; do not estimate
it.} It is not identified jointly with the cost parameters, entering
only through \eqn{(1-\beta F)(1-L)}. Use \code{1/(1+r)} with a \emph{real}
quarterly rate: the loss is quadratic in log deviations, which are
unit-free, so the real rate is the relevant one even when \code{y} and
\code{ystar} are nominal. The default of \code{1} is admissible provided
\eqn{\rho(G) < 1}, but it means indifference between the present and the
arbitrarily distant future and it lengthens the effective lead horizon;
it warns unless \code{quiet = TRUE}.}

\item{data}{data.frame ordered in time, \strong{with no gaps}. Not checked --
a gap will silently corrupt every lag. Roadmap item R-7.}

\item{m}{integer, order of the adjustment cost polynomial. The equation
carries \code{m - 1} lags of \code{dy}.}

\item{cost}{\code{"geometric"} (\code{k_j = kappa * psi^(j-1)}, two parameters) or
\code{"free"} (\code{m} parameters). \code{"free"} warns above \code{m = 4}; see the
variance-amplification note in Details.}

\item{expectations}{formula or character vector of extra VAR variables.
\code{d(ystar)} is always included and always occupies position 1.}

\item{var_lags}{integer lag order of the auxiliary VAR. \strong{This also sets
GMM instrument dating} through the default \code{iv_lag = var_lags + 2}. A
longer VAR buys forecast accuracy and pays in instrument strength: on
178 quarterly observations first-stage \code{F} was 13.1 at \code{p = 1}, 6.0 at
\code{p = 2} and 3.3 at \code{p = 4}, and dating the instruments correctly costs
more strength again.}

\item{diff_exp}{logical; difference the \code{expectations} variables before
they enter the VAR.}

\item{method}{\code{"nls"} (default) or \code{"gmm"} (two-step, Newey-West).}

\item{instruments}{formula or character vector of \emph{excluded} instruments
for GMM, added to the automatic set. These are taken as supplied -- the
\code{iv_lag} rule is \strong{not} applied to them, so date them yourself.
Collinear columns are dropped by QR pivot.}

\item{iv_lag}{integer, the lag at which those \emph{automatic} GMM instruments
that are built from \code{y} or \code{ystar} are dated -- the error-correction
term, the lags of \code{dy}, and the \code{d(ystar)} states. \code{NULL} (default)
uses \code{var_lags + 2}, the smallest lag the dating rule permits. Other
state variables are exogenous with respect to measurement error in
\code{ystar} and stay at \code{t-1} whatever \code{iv_lag} is set to.

The composite error carries measurement error \code{nu} at \code{t-1} through
\code{t - var_lags - 1}: the error-correction term contributes \verb{nu_\{t-1\}},
and the forward term contributes \code{d(ystar)} at \verb{t-1 .. t-var_lags}.
Anything built from \code{y} or \code{ystar} is therefore only predetermined at
\code{t - (var_lags + 2)} or earlier.

\code{iv_lag = 1} puts the equation's own regressors back in the instrument
set. That is more efficient, and correct \strong{only if \code{ystar} is measured
without error} -- which it usually is not, since it normally comes
from a first-stage cointegrating regression. It warns. See the
Instrument dating section for the size of the trade.}

\item{free_forward}{logical. If \code{TRUE}, \code{a_f} is estimated freely instead
of being restricted to \code{sum(d_i)}. Comparing the free estimate to
\code{sum(d_i)} evaluated at the estimated \code{alpha} is the direct test of the
Euler restriction, and is the single most informative diagnostic the
package offers.}

\item{growth}{character(1), column name of trend growth. Adds the
growth-neutrality correction \verb{(1 - sum a_i - sum d_i) * g_t} to the
forward term. See Details -- this is a deliberate departure from the
exact optimum.}

\item{hac_lags}{integer Newey-West bandwidth; \code{NULL} uses
\code{floor(4 * (T/100)^(2/9))}.}

\item{start}{numeric vector of starting values on the optimisation scale.}

\item{subset}{logical or integer vector selecting rows of \code{data}.}

\item{maxit}{integer, optimiser iteration limit.}

\item{restarts}{integer, number of times Nelder-Mead is re-run from its
own solution. This matters: a single pass routinely stops short.}

\item{quiet}{logical; suppress the \code{beta = 1} warning.}
}
\value{
An object of class \code{recmfit}, a list with \code{theta}, \code{par}, \code{vcov},
\code{alpha}, \code{a}, \code{k}, \code{scalars}, \code{a_f}, \code{delta}, \code{residuals}, \code{fitted},
\code{dep}, \code{index}, \code{n}, \code{npar}, \code{var}, \code{extras} and \code{converged}, plus two
closures:

\describe{
\item{\code{alpha_fn(theta)}}{maps any \code{theta} to \code{alpha}; powers
delta-method standard errors.}
\item{\code{objective(theta)}}{the criterion at any \code{theta}; powers
\code{\link[=recm_profile]{recm_profile()}}.}
}

\strong{The closures capture \code{data} by reference}, so a \code{recmfit} is not
portable across sessions without its data. That is a deliberate trade.
}
\description{
Fits the forward-looking error-correction specification used in the
Federal Reserve Board's FRB/US model and the Bank of Canada's LENS model.
Given a decision variable, a frictionless target and a \strong{calibrated}
discount factor, \code{recm_estimate()} recovers the adjustment cost weights
and reports the implied decision rule.
}
\details{
The fitted decision rule is reported in two equivalent formats.

\emph{Explicit} -- the coefficient on the forward sum is 1 by construction:

\deqn{\Delta y_t = a_0 (y - y^*)_{t-1}
  + \sum_{i=1}^{m-1} a_i \Delta y_{t-i}
  + \sum_{i \geq 0} d_i E_{t-1}[\Delta y^*_{t+i}] + \delta'W_t + e_t}

\emph{Compressed} (LENS / FRB-US presentation), with
\eqn{f_i = d_i / \sum_j d_j} summing to 1 and \eqn{a_f = \sum_j d_j}
under the Euler restriction:

\deqn{\Delta y_t = a_0 (y - y^*)_{t-1}
  + \sum_{i=1}^{m-1} a_i \Delta y_{t-i}
  + a_f \sum_{i \geq 0} f_i E_{t-1}[\Delta y^*_{t+i}] + \delta'W_t + e_t}
}
\section{Identification}{

This is inverse optimal control, and weak identification is the normal
case rather than the exception. At \code{m = 2} a fourfold change in \code{k_1/k_0}
cost 2.2\% of SSR. \strong{Point estimates alone are not a defensible output} --
run \code{\link[=recm_profile]{recm_profile()}} and \code{\link[=recm_boot]{recm_boot()}}.

High-order differences also explode: \eqn{Var((1-L)^j y)} grows like
\eqn{\binom{2(j-1)}{j-1}}, about 3.5e10 at \code{j = 20}, so cost parameters
would have to span ten orders of magnitude to contribute comparably.
\code{cost = "geometric"} exists for exactly this reason.
}

\section{Growth neutrality}{

On a balanced growth path with \eqn{\Delta y^* = g}, the exact Euler
solution satisfies
\eqn{y - y^* = g(1 - \sum_{i \geq 1} a_i - \sum_i d_i)/a_0 < 0}. The
optimum runs \emph{below} a growing target: the agent optimally lags, because
moving is costly. This is a property of the optimum, not a bug. Setting
\code{growth} adds a correction that removes it, matching FRB/US practice --
a deliberate departure from the exact optimum, and it should not be
presented as "the solution".
}

\section{Instrument dating}{

\code{iv_lag} trades bias against variance, and the default deliberately buys
bias protection at a real cost in precision.

Monte Carlo on the reference DGP (\code{m = 2}, \code{var_lags = 2}, so the default
\code{iv_lag} is 4; RMSE for \eqn{a_0}, 60 replications at \code{T = 178} and 12 at
\code{T = 3000}):

\tabular{lrrr}{
\strong{measurement error in ystar} \tab \strong{NLS} \tab
\strong{\code{iv_lag = 1}} \tab \strong{\code{iv_lag = 4}} \cr
none, \code{T = 178}      \tab 0.0204 \tab 0.0212 \tab 0.0541 \cr
none, \code{T = 3000}     \tab 0.0037 \tab 0.0035 \tab 0.0273 \cr
severe, \code{T = 178}    \tab 0.0889 \tab 0.0880 \tab 0.0549 \cr
severe, \code{T = 3000}   \tab 0.0871 \tab 0.0876 \tab 0.0132
}

The bottom row is the one that matters. With \code{sd(nu)} at 0.02, NLS and
\code{iv_lag = 1} both sit at a bias of about +0.087 that \strong{does not shrink
with \code{T}} -- they are inconsistent, and the contaminated instruments buy
nothing over NLS. At the default dating the bias is -0.009 and falling.
The two failure modes are not symmetric: dating too early gives a wrong
answer that more data never fixes, dating correctly gives a noisy answer
that more data does fix. That is why the rule is the default even though
it costs a factor of 2.6 in RMSE when there is no measurement error to
correct.

Validity was checked directly: at \code{iv_lag = var_lags + 2} the residual is
orthogonal to every instrument column (\code{p > 0.05} on all of them at
\code{T = 3000}).

The exemption matters as much as the dating. Lagging the \emph{whole} state
vector, exogenous expectations variables included, destroys
identification -- the criterion goes numerically flat and \eqn{a_0}
stalls near -0.11 whatever \code{T} is. Only the \code{y}/\code{ystar}-derived columns
carry the rule; the rest stay at \code{t-1}, where they are strong.

Set \code{iv_lag = 1} if and only if \code{ystar} is measured exactly.
}

\section{Standard errors}{

\code{Z_t} is a generated regressor: it depends on the estimated auxiliary VAR
\emph{and} on \code{ystar}, which usually comes from a first-stage cointegrating
regression. In Monte Carlo the conditional standard errors came in at
roughly half the true sampling standard deviation for the PAC parameters,
while the standard error on an ordinary exogenous regressor was correct.
Use \code{\link[=recm_boot]{recm_boot()}}.
}

\examples{
set.seed(1)
n <- 160
dys <- as.numeric(stats::filter(rnorm(n, 0.0104, 0.004), 0.5,
                                method = "recursive"))
dat <- data.frame(ystar = cumsum(dys))
dat$y <- dat$ystar - 0.05 + cumsum(rnorm(n, 0, 0.002))

fit <- recm_estimate("y", "ystar", beta = 0.995, data = dat, m = 2,
                     var_lags = 2, restarts = 1, quiet = TRUE)
print(fit)

}
\seealso{
\code{\link[=recm_profile]{recm_profile()}}, \code{\link[=recm_boot]{recm_boot()}}, \code{\link[=equation]{equation()}}, \code{\link[=lead_weights]{lead_weights()}};
\code{docs/02-math-spec.md} for the underlying mathematics.
}
