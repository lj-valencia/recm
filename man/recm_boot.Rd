% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{recm_boot}
\alias{recm_boot}
\title{Bootstrap standard errors that propagate VAR estimation error}
\usage{
recm_boot(object, R = 299, var_error = TRUE, eq_error = TRUE, seed = NULL)
}
\arguments{
\item{object}{an object of class \code{recmfit}.}

\item{R}{integer, number of bootstrap replications.}

\item{var_error}{logical; recursively resample the auxiliary VAR, giving
a fresh \verb{H*} each replication.}

\item{eq_error}{logical; wild-bootstrap the equation error (Rademacher
weights).}

\item{seed}{integer or \code{NULL}; passed to \code{\link[=set.seed]{set.seed()}}.}
}
\value{
A list of class \code{recm_boot} with \code{cov} (the bootstrap covariance
of the parameter vector), \code{replicates} (an \code{R} by \code{npar} matrix, failed
replications removed), \code{se}, \code{interval} (percentile), \code{R_ok} and
\code{R_failed}.
}
\description{
The conditional standard errors reported by \code{\link[=summary]{summary()}} treat the
auxiliary VAR as known. This function relaxes that by resampling both the
VAR and the equation error and re-running the whole pipeline per
replication.
}
\details{
Each replication simulates the auxiliary VAR forward from resampled
residuals, rebuilds \code{ystar} by cumulation, regenerates \code{y} from the
fitted decision rule with a wild-bootstrapped error, and re-estimates.

\strong{Deviation from \code{docs/03-api-reference.md}:} that document describes
the equation step as a \emph{fixed-design} wild bootstrap. A fixed design is
not available here -- the error-correction regressor is built from \code{y},
so perturbing \code{y} necessarily moves the design. This implementation is
therefore a recursive (dynamic) bootstrap, which is the correct
construction for a dynamic equation. Roadmap item R-1.
}
\section{What this does not cover}{

Uncertainty in \code{ystar} is \strong{not} propagated beyond its VAR
representation. \code{ystar} normally comes from a cointegrating regression
estimated outside the package. Superconsistency makes this second-order
asymptotically, but at \code{T} around 170 quarters it is not zero. Roadmap
item R-1.

In Monte Carlo, the conditional standard errors came in at roughly half
the true sampling standard deviation; this is the intended fix, but its
own calibration is not yet verified (docs/04-testing.md, "What is not
tested yet").
}

\examples{
set.seed(1)
n <- 120
dys <- as.numeric(stats::filter(rnorm(n, 0.0104, 0.004), 0.5,
                                method = "recursive"))
dat <- data.frame(ystar = cumsum(dys))
dat$y <- dat$ystar - 0.05 + cumsum(rnorm(n, 0, 0.002))
fit <- recm_estimate("y", "ystar", beta = 0.995, data = dat, m = 2,
                     var_lags = 2, restarts = 0, quiet = TRUE)
bs <- recm_boot(fit, R = 3, seed = 1)
bs$R_ok

}
\seealso{
\code{\link[=recm_profile]{recm_profile()}}, \code{\link[=recm_estimate]{recm_estimate()}}.
}
