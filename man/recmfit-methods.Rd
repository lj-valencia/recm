% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{recmfit-methods}
\alias{recmfit-methods}
\alias{coef.recmfit}
\alias{vcov.recmfit}
\alias{residuals.recmfit}
\alias{fitted.recmfit}
\alias{confint.recmfit}
\alias{logLik.recmfit}
\alias{print.recmfit}
\alias{summary.recmfit}
\alias{print.summary.recmfit}
\alias{plot.recmfit}
\title{Methods for fitted rational error correction models}
\usage{
\method{coef}{recmfit}(object, ...)

\method{vcov}{recmfit}(object, ...)

\method{residuals}{recmfit}(object, ...)

\method{fitted}{recmfit}(object, ...)

\method{confint}{recmfit}(object, parm, level = 0.95, ...)

\method{logLik}{recmfit}(object, ...)

\method{print}{recmfit}(x, ...)

\method{summary}{recmfit}(object, digits = 4, ...)

\method{print}{summary.recmfit}(x, digits = x$digits, ...)

\method{plot}{recmfit}(x, horizon = 24, level = 0.95, ...)
}
\arguments{
\item{object, x}{an object of class \code{recmfit}, or of class
\code{summary.recmfit} for the corresponding print method.}

\item{...}{further arguments passed to or from other methods.}

\item{parm, level}{passed to \code{\link[=confint]{confint()}}; \code{parm} selects parameters and
\code{level} is the confidence level.}

\item{digits}{integer, significant digits used in printing.}

\item{horizon}{integer, highest lead index shown by \code{plot()}.}
}
\value{
\code{coef()} returns the named parameter vector, \code{vcov()} its
covariance, \code{residuals()} and \code{fitted()} numeric vectors over the
retained sample \code{object$index}, \code{confint()} a two-column matrix, and
\code{logLik()} an object of class \code{logLik} computed under Gaussian errors.
\code{print()} returns its argument invisibly. \code{summary()} returns an object
of class \code{summary.recmfit}. \code{plot()} is called for its side effect and
returns the lead-weight data frame invisibly.
}
\description{
Methods for fitted rational error correction models
}
\details{
\code{summary()} prints, in one call:
\enumerate{
\item estimated parameters on the optimisation scale, \code{printCoefmat} style
\item cost parameters on the natural scale, delta method
\item \strong{Format 1, explicit}: \code{a_0}, \verb{a_1 ... a_\{m-1\}}, \code{delta}, with the
forward coefficient fixed at 1
\item \strong{Format 2, compressed}: \code{a_0}, \verb{sum a_i}, \code{a_f}, mean lead,
half-life, and the \code{f_i} table
\item fit and diagnostics: residual standard error, R-squared, Ljung-Box,
Durbin-Watson, \code{rho(G)*rho(H)}, cost admissibility, Hansen J,
first-stage F
}

The generated-regressor caveat is printed unconditionally.
}
\seealso{
\code{\link[=recm_estimate]{recm_estimate()}}, \code{\link[=equation]{equation()}}, \code{\link[=lead_weights]{lead_weights()}},
\code{\link[=recm_profile]{recm_profile()}}.
}
